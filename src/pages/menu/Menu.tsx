import { ScrollView, Text } from 'react-native'

const Menu = () => {
  return (
    // eslint-disable-next-line react/react-in-jsx-scope
    <ScrollView>
      {/* eslint-disable-next-line react/react-in-jsx-scope */}
      <Text>Menu ~</Text>
    </ScrollView>
  );
};

export default Menu;
